{
	// See https://go.microsoft.com/fwlink/?LinkId=733558
	// for the documentation about the tasks.json format
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Compilador de archivo *.java",
			"type": "shell",
			"command": "javac -d 'bin/' -cp 'bin/' '${relativeFile}'",
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}